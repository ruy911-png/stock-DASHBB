# stock-DASHBB

Stock dashboard for Stock King.

## Development

The deployed `index.html` contains an encoded dashboard template. Its editable
source lives at `src/dashboard.template.html`.

```text
npm run check
npm run build
```

After editing the source, run `npm run build` and commit both the source and the
generated `index.html`.
