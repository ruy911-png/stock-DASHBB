# stock-DASHBB (주식킹)

## 종목분석 리포트 컨벤션

`data/analyses-backup.json`에 종목분석을 추가/수정할 때 지키는 표준 형식.

### 섹션 구성 (5개, 이 순서)
1. **기업개요** — 사업모델, 매출구성, 경쟁지위
2. **경제적해자** — 실제 경쟁우위 + 그 우위를 위협하는 리스크(규제/기술변화 등)를 함께 서술
3. **기본적분석** — 최근 실적 흐름, 업황/사이클, 투자논지(강세근거/약세리스크)
   - **현금흐름 분석은 상시 포함** — 영업현금흐름(OCF)·잉여현금흐름(FCF) 추이, capex가 FCF에 미치는 영향을 매번 다룰 것 (생략 금지)
4. **기술적분석** — 차트 추세, 이동평균선, 거래량, 지지/저항 (사용자 제공 차트가 있으면 그걸로, 없으면 미확인)
5. **밸류에이션** — PER, 목표주가
   - **PBR 관점 판단도 상시 포함** — 현재 PBR(주당순자산 BPS 포함)과 자체 과거평균 또는 동종업계 비교를 매번 다룰 것 (PER만 쓰고 PBR을 생략하지 말 것)

### 작성 규칙
- 섹션당 최대 3개 bullet, bullet당 인용 출처 최대 1개 (괄호로 끝에, 예: `(SEC 10-K, 2026)`)
- 확인 불가능한 수치는 "미확인"으로 표기 — 지어내지 않음
- **매수/매도 추천 절대 금지** — 애널리스트 컨센서스·목표주가는 "사실 보고"로만 인용, 내 의견처럼 포장하지 않음
- 한국 종목: 네이버금융 / 미국 종목: Yahoo Finance·Investing.com 등 실제 웹검색으로 최신 수치 확인 후 작성 (학습지식만으로 추정 금지)
- 미국주식 entry는 관례상 `price: 0, chg: 0` (실시간 가격은 이 파일이 아닌 별도 소스에서 주입됨)

### 파이프라인
- `data/analyses-backup.json` 수정 → `npm run build` (src/dashboard.template.html → index.html) → `npm run check` (빌드 동기화 + 시황데이터 검증 + 수집기 테스트) 순으로 검증 후 커밋
- 브랜치가 이미 머지된 상태면 `git fetch origin main && git checkout -B <branch> origin/main`로 재시작 후 이어서 작업 (머지된 브랜치에 새 커밋을 쌓지 않음)

### 자동머지 정책
- **시황 자동수집 PR(`automation/market-data-*`)에 한해서만** 자동머지 허용 (`.github/workflows/collect-market-data.yml`)
- 그 외 모든 PR(종목분석 추가, 기능/레이아웃 수정 등)은 사용자가 명시적으로 요청할 때만 머지 — 임의로 자동머지하지 않음
